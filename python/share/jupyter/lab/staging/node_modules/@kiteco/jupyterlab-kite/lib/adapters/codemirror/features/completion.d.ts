import * as CodeMirror from 'codemirror';
import { CodeMirrorLSPFeature } from '../feature';
export declare class Completion extends CodeMirrorLSPFeature {
    name: string;
    private _completionCharacters;
    get completionCharacters(): string[];
    afterChange(change: CodeMirror.EditorChange): void;
}
