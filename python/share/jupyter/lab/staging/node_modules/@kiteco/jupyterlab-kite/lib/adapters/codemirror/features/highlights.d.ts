import * as CodeMirror from 'codemirror';
import * as lsProtocol from 'vscode-languageserver-protocol';
import { CodeMirrorLSPFeature, IFeatureCommand } from '../feature';
export declare class Highlights extends CodeMirrorLSPFeature {
    name: string;
    protected highlight_markers: CodeMirror.TextMarker[];
    static commands: Array<IFeatureCommand>;
    register(): void;
    remove(): void;
    protected clear_markers(): void;
    protected handleHighlight: (items: lsProtocol.DocumentHighlight[], documentUri: string) => void;
    protected onCursorActivity: () => Promise<void>;
}
