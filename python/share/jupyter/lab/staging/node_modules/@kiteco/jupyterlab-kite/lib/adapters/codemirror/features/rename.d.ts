import * as lsProtocol from 'vscode-languageserver-protocol';
import { CodeMirrorLSPFeature, IEditOutcome, IFeatureCommand } from '../feature';
export declare class Rename extends CodeMirrorLSPFeature {
    name: string;
    static commands: Array<IFeatureCommand>;
    handleRename(workspaceEdit: lsProtocol.WorkspaceEdit): Promise<IEditOutcome>;
}
