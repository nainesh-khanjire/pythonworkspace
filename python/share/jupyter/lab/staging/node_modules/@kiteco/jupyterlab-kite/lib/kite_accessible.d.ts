import { ServiceManager } from '@jupyterlab/services';
import { ISettingRegistry } from '@jupyterlab/settingregistry';
import { ListModel } from '@jupyterlab/extensionmanager';
import { DocumentConnectionManager } from './connection_manager';
import '../style/kite_accessible.css';
export declare class KiteAccessible extends ListModel {
    private connectionManager;
    static CreateAsync: (serviceManager: ServiceManager, registery: ISettingRegistry, connectionManager: DocumentConnectionManager) => Promise<KiteAccessible>;
    constructor(serviceManager: ServiceManager, settings: ISettingRegistry.ISettings, connectionManager: DocumentConnectionManager);
    checkHealth(): Promise<void>;
    private notifyHealth;
    private getHealth;
    private trackIncompatiblity;
    private fetchKiteInstalled;
    private get kiteInstalledUrl();
}
