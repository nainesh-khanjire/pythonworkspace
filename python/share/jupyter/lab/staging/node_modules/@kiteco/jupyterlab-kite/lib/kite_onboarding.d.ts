import { JupyterFrontEnd } from '@jupyterlab/application';
import { ICommandPalette } from '@jupyterlab/apputils';
import { IDocumentManager } from '@jupyterlab/docmanager';
import { IStateDB } from '@jupyterlab/statedb';
import { DocumentConnectionManager } from './connection_manager';
export declare const onboardingShownKey = "onboardingShown";
export declare class KiteOnboarding {
    private app;
    private palette;
    private documentManager;
    private state;
    private connectionManager;
    constructor(app: JupyterFrontEnd, palette: ICommandPalette, documentManager: IDocumentManager, state: IStateDB, connectionManager: DocumentConnectionManager);
    registerCommand(): void;
    showOnBoot(): Promise<void>;
    _fetch(): Promise<string>;
    private _show;
}
