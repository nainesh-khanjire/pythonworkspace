import { CodeMirrorEditor } from '@jupyterlab/codemirror';
import { CodeJumper } from '../../jumpers/jumper';
import { IEditorExtension, KeyModifier } from '../editor';
import { CodeMirrorTokensProvider } from './tokens';
export declare class CodeMirrorExtension extends CodeMirrorTokensProvider implements IEditorExtension {
    jumper: CodeJumper;
    static modifierKey: KeyModifier;
    constructor(editor: CodeMirrorEditor, jumper: CodeJumper);
    static configure(): void;
    connect(): void;
    selectToken(lookupName: string, target: HTMLElement): import("@jupyterlab/codeeditor").CodeEditor.IToken;
    static _countUsagesBefore(lookupName: string, target: Node, classFilter: string, classFilterOn: boolean): number;
}
