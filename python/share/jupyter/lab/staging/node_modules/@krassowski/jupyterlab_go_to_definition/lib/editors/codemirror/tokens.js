export class CodeMirrorTokensProvider {
    constructor(editor) {
        this.editor = editor;
    }
    getTokens() {
        return this.editor.getTokens();
    }
    getTokenAt(offset) {
        let position = this.editor.getPositionAt(offset);
        return this.editor.getTokenForPosition(position);
    }
}
//# sourceMappingURL=tokens.js.map